# Deep learning
torch>=2.0
transformers>=4.30.0        # ESM2 via HuggingFace (AutoTokenizer, AutoModel)

# Bioinformatics
biopython>=1.83             # Bio.Phylo, Bio.SeqIO, Bio.Seq, Bio.SeqRecord
ete3>=3.1.3                 # phylogenetic tree manipulation
augur>=22.0.0               # nextstrain augur (refine, ancestral) — called via subprocess

# Scientific computing
numpy>=1.26.0
scipy>=1.14.0

# Data
pandas>=2.2.0

# Utilities
tqdm>=4.66.0
requests>=2.31.0
pyyaml>=6.0

# Dev / testing
pytest>=7.0.0

# Benchmark suite (tree-generation table) — treesbm env only; external baselines
# (ARTreeFormer, PhyloVAE) live in their own cloned repos + envs (see benchmarks/EXTERNAL.md)
dendropy>=4.6.1            # birth-death topology simulation (BD baselines, sim track)
pyvolve>=1.1.0            # sequence evolution under neutral/JTT/WAG/LG
tqdist>=1.0.0            # normalized quartet distance
